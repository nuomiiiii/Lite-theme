---
description: Lite-theme 独立主题仓的发版规则。
alwaysApply: true
---

# Lite-theme 流程

公共大屏主题独立仓。口令跟工作区同一套：`上传`、`发版`、`发稳定`。未说发版就只改代码。

本地开发禁止 `git push`。没明确表示：修改和验证一律只在本地。

远端：https://github.com/nuomiiiii/Lite-theme 。未说口令禁止 push。

这是 Lite 原生主题，只给 Lite 用。识别文件是 `Lite-theme.json`，不是主题显示名（`name` 仍是 `Lite-Theme`）。Lite 优先读 `Lite-theme.json`，找不到再兼容 `komari-theme.json`。本仓不要写 `komari-theme.json`。`url` 填本仓 GitHub 地址，管理后台「从原 URL 更新」会按 GitHub Release 第一个资源下载。发版时把主题 zip 作为 Release 第一个 asset。发版只写面向用户的主题变更。GitHub 文本 LF，禁止 AI/Cursor 标记，禁止把本地调试记录写进 commit、PR、Release。
